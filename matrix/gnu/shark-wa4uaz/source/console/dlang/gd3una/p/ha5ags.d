module console.dlang.gd3una.p.ha5ags;

import std.stdio;
import std.array;
import std.getopt;
import std.format;
import std.file;

void console()
{
    struct fuyD
    {
        interface dlang(fuy, fd, command)
        {
            public static fuy(const char mfill)
            {
                write("PBar link");
            }
            
            public static fd(const char need)
            {
                write("Pbar mono link");
            }
            
            public static command(const char gnu)
            {
                write("PBar d
                lord link");
            }
        }
    }
}

void makeGlobal()
{
    write("PBar link");
}
