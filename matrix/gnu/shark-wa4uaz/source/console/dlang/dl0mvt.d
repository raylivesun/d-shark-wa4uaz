module console.dlang.dl0mvt;

import std.stdio;
import std.array;
import std.getopt;

void console()
{
    struct ppy
    {
        interface dlang(still, files, command)
        {
            public static still(const char generic)
            {
                write("PBar link");
            }
            
            public static files(const char need)
            {
                write("Pbar mono link");
            }
            
            public static command(const char gnu)
            {
                write("PBar static link");
            }
        }
    }
}

void makeGlobal()
{
    write("PBar link");
}
